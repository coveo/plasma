---
'@coveord/plasma-mantine': minor
'@coveord/plasma-llms': patch
---

Select ranges of `Table` rows with Shift-click

With multi-row selection enabled, select a row checkbox and then Shift-click another checkbox to select every
selectable row between them on the displayed page. Existing selections outside the range are preserved.
