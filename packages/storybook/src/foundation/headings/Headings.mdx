import { faker } from '@faker-js/faker';
import { Meta, Story } from '@storybook/addon-docs/blocks';
import { Headings } from './Headings.stories';

<Meta title="@foundation/Headings" />

# Headings

The Plasma theme defines the following heading sizes. The font family for all headings is `canada-type-gibson, sans-serif`.

{faker.lorem.paragraphs(2)}

<Story of={Headings} />
