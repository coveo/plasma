import {DatePickerInput} from '@mantine/dates';

DatePickerInput.displayName = 'DatePickerInput';

export {DatePickerInput, type DatePickerInputFactory, type DatePickerInputProps} from '@mantine/dates';
