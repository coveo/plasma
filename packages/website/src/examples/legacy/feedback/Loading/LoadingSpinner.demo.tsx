import {LoadingSpinner} from '@coveord/plasma-react';

const Demo = () => (
    <>
        <LoadingSpinner size={16} />
        <LoadingSpinner />
        <LoadingSpinner size={32} />
    </>
);
export default Demo;
