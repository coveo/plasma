name: Publish
description: Publish a new version of the packages on npm
runs:
    using: composite
    steps:
        - name: Publish
          shell: bash
          run: DEBUG=* node ./.github/actions/publish/publishNewVersion.mjs
