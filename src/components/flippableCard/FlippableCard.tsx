import * as React from 'react';

export interface IFlippableCardProps { }

export class FlippableCard extends React.Component<IFlippableCardProps> {
  render() {
    return (
      <div>Not implemented yet.</div>
    );
  }
}
